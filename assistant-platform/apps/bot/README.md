# Telegram Bot (MVP)

Запуск локально:

```bash
cd assistant-platform/apps/bot
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
# установи переменную окружения с токеном бота
python -m bot.main
```

Пока это заглушка. Следующий шаг: добавить кнопки для открытия WebApp и router.
